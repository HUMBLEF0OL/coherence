# v0.2 Spec Freeze (2026-05-09)

This document enumerates the v0.2 corpus exactly as it stood when M0 closed. Every later milestone is bound to the IDs below; new IDs may not be coined within v0.2 except as DD-093+.

## Design Decisions DD-065..DD-092 (28 entries)

| ID | Title (short) |
|----|---------------|
| DD-065 | Author proposal-only quarantine boundary |
| DD-066 | Trickle scan-cache reservation (state.json only in v0.2) |
| DD-067 | Author separate pipeline + staged adoption (planner gated by alpha telemetry) |
| DD-068 | v0.1.1 telemetry events (`tool_invocation_signature`, `user_prompt_signature`, `agent_response_id`) |
| DD-069 | Annotate anchor format byte-for-byte + `auto-annotated: true` discriminator + sidecar fallback |
| DD-070 | Statusline integration: hybrid (`state-snapshot.json` consumer) |
| DD-071 | OSC 8 click affordance with three-tier graceful degradation |
| DD-072 | Quarantine directory disambiguation (`proposals/` for trust isolation; `quarantine/` for state-corruption recovery) |
| DD-073 | Annotate opt-in: hybrid (global mode + denylist + per-doc command) |
| DD-074 | `graduation.json` scopes (per-doc / per-dir / global; most-specific-wins) |
| DD-075 | Proposal expiry fences (14 d / 7 d quiet / 5 consecutive ignored) |
| DD-076 | Bash repetition threshold (3 normalised matches in 30 min) |
| DD-077 | File-creation threshold (3 files + locality + Jaccard ≥ 0.8) |
| DD-078 | Agent-correction threshold (5-min window, line ratio ≥ 0.20, 3 occurrences in 7-day window) |
| DD-079 | Reserved (intentional numbering gap) |
| DD-080 | v1→v2 migrator (single coordinated atomic step) |
| DD-081 | Acceptance UX dedicated `/coherence:propose-*` slash-command set |
| DD-082 | Name-collision policy (refuse + `--rename` / `--overwrite <retyped-path>`) |
| DD-083 | Proposal revert via `git revert <accept-commit-sha>` |
| DD-084 | Snapshot debounced writer (5 s minimum interval, hot-path-zero-cost) |
| DD-085 | Cost ceiling = v0.1 baseline × 1.30 (60/30/10 Author/Annotate/Trickle partition) |
| DD-086 | `share-metrics --anonymized` file-write only in v0.2 (egress deferred to v0.3) |
| DD-087 | `proposal.schema.json` validated at writer + read time |
| DD-088 | Proposal lifecycle FSM `queued → surfaced → ignored | accepted | rejected | reverted | expired` |
| DD-089 | `signal-cache.json` per-bucket caps (bash 500, file-creation 500, agent-correction 200) |
| DD-090 | `host-capabilities.json` v0.2 fields (`claude_url_scheme_supported`, `terminal_hyperlink`) |
| DD-091 | Author/Annotate LLM contract (model `claude-sonnet-4-5-20251022`, temperature 0, prompts under `prompts/v2/`) |
| DD-092 | v0.2.1 calibration patch commitment (re-tunes DD-076/077/078 against `proposal_signal_observed { kind, would_have_fired }`) |

## Calibration commitment chain

DD-076 + DD-077 + DD-078 ship with default-locked thresholds. `proposal_signal_observed` shadow events feed the DD-092 v0.2.1 patch, which re-tunes the thresholds with Wilson 95% confidence intervals.

## Deferral list (v0.3+)

- Auto-apply / graduated trust ladder for accepted proposals (v1.0).
- Egress / opt-in HTTPS upload of anonymised metrics (v0.3).
- Plugin marketplace packaging, team-shared `coherence-ignore`, monorepo `scope:` declarations (v0.3).
- Cross-session pattern learning beyond a single 7-day rolling window (v1.0).
- `/coherence:audit` deep audit + assertion checking (v1.0).
- `/coherence:de-annotate` (v0.3).
- Per-file scan tombstones under `scan-cache/<hash>.json` (v0.3).
- Author-pipeline planner / proposal consolidation stage (v0.3 unless v0.2-alpha telemetry meets DD-067 trigger).
