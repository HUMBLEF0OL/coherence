# Dependencies
node_modules/
jspm_packages/

# Logs and Debugging
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Runtime data
pids
*.pid
*.seed
*.pid.lock

# Coverage and Build Output
coverage/
.nyc_output
dist/
build/Release

# Environment Variables
.env
.env.local
.env.production.local

# IDE/Editor
.vscode/
.eslintcache
.mcp.json
graphify-out

# Coherence runtime + local scratch state
.claude/coherence/
.claude/settings.local.json

# npm pack build artifacts (release-asset territory, not version control)
*.tgz

# v0.4 M4 — autogen command stubs (build artifacts).
# Anchored to repo root so src/commands/ remains tracked.
/commands/
.coherence-stub-hash
