# monorepo-5 — small synthetic fixture (M1)

5-package layout used by ship-time CI for M-SCOPE-1. Generated on demand by
the same `buildMonorepoFixture()` helper as monorepo-100. Used in
`tests/integration/scope-debug-command.test.ts` to keep the round-trip cheap.
