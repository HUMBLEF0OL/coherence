{
  "metadata": {
    "type": "page"
  },
  "title": "Design Decisions",
  "url": "https://www.notion.so/35d010d46a7081e58c1bcb14fd4b8ea5",
  "text": "Here is the result of \"view\" for the Page with URL https://www.notion.so/35d010d46a7081e58c1bcb14fd4b8ea5 as of 2026-05-11T14:59:24.634Z:\n<page url=\"https://www.notion.so/35d010d46a7081e58c1bcb14fd4b8ea5\">\n<ancestor-path>\n<parent-page url=\"https://www.notion.so/35d010d46a7081d687d8f32f4a25f500\" title=\"v0.4\"/>\n<ancestor-2-page url=\"https://www.notion.so/35b010d46a7081688d3fe27c531626b6\" title=\"Releases\"/>\n<ancestor-3-page url=\"https://www.notion.so/93d010d46a708280ba6c013d97211fd6\" title=\"Coherence\"/>\n<ancestor-4-data-source url=\"collection://c19010d4-6a70-82d2-bd12-87ccaf25544f\" name=\"Projects\"/>\n<ancestor-5-database url=\"https://www.notion.so/d73010d46a70820a9a2781c14c2a368b\" title=\"Projects\"/>\n<ancestor-6-page url=\"https://www.notion.so/088010d46a70830dbf2001b2bb84264e\" title=\"Work\"/>\n</ancestor-path>\n<properties>\n{\"title\":\"Design Decisions\"}\n</properties>\n<content>\n**Parent:** [v0.4](https://www.notion.so/35d010d46a7081d687d8f32f4a25f500) · **Status:** Open register · 2026-05-11\n> Continues v0.3's design-decision register. v0.3 ratified **24 DDs (DD-093..DD-118 active + 4 retired/superseded)** — 16 new spec DDs and 2 architectural commitments (DD-117 no backend, DD-118 no legacy support). v0.4 starts at **DD-119**. Format mirrors v0.3: each entry has a one-line statement, the OQ that motivated it, the rejected alternative(s), the v0.3 codebase / spec evidence cited, the BRD slot, and an audit note.\n---\n## Status legend\n- 🟢 **Ratified** — entry is normative for v0.4 spec authoring\n- 🟡 **Draft** — statement written, awaiting cross-check against v0.3 implementation\n- 🔴 **Open** — placeholder; pending OQ resolution in [Open Questions](https://www.notion.so/35d010d46a7081d687d8f32f4a25f500)\n---\n## Numbering\n- v0.1: DD-001..DD-064\n- v0.2: DD-065..DD-092 (27 ratified, DD-079 vacated)\n- v0.3: DD-093..DD-118 (24 active + 4 retired/superseded)\n- v0.4: **DD-119 onward**\n---\n## Allocation queue (provisional — will be filled as OQs resolve)\n<table header-row=\"true\">\n<tr>\n<td>DD</td>\n<td>Source OQ</td>\n<td>Subject</td>\n<td>v0.3 dependency</td>\n<td>Type</td>\n</tr>\n<tr>\n<td>DD-119</td>\n<td>OQ-v4-03</td>\n<td>Manifest path = `.claude-plugin/plugin.json`</td>\n<td>(none — Anthropic plugin schema requirement)</td>\n<td>New DD</td>\n</tr>\n<tr>\n<td>DD-120</td>\n<td>OQ-v4-01</td>\n<td>State-storage tri-partition rule</td>\n<td>DD-109 (signals stay per-developer); v0.3 state files inventory</td>\n<td>New DD</td>\n</tr>\n<tr>\n<td>DD-121</td>\n<td>OQ-v4-08</td>\n<td>Version field strategy = explicit semver</td>\n<td>v0.3 `\"version\": \"0.3.0\"` pattern</td>\n<td>New DD</td>\n</tr>\n<tr>\n<td>DD-122</td>\n<td>OQ-v4-02</td>\n<td>Manifest layout migration = strict re-install</td>\n<td>DD-118 (no legacy migration); v0.3 refuseLegacy.ts pattern</td>\n<td>New DD</td>\n</tr>\n<tr>\n<td>DD-123</td>\n<td>OQ-v4-03</td>\n<td>`claude plugin validate` in `release-ga` preflight</td>\n<td>v0.3 M6 static-analysis gates (`scripts/release-ga.mjs`)</td>\n<td>New DD</td>\n</tr>\n<tr>\n<td>DD-124</td>\n<td>(post-M8 audit B2)</td>\n<td>`parseMajor` correctness for ≥1.0.0</td>\n<td>v0.3 `src/state/refuseLegacy.ts` parseMajor formula</td>\n<td>New DD</td>\n</tr>\n<tr>\n<td>DD-125</td>\n<td>OQ-v4-11</td>\n<td>Minimal `/coherence:audit` = bundling-only</td>\n<td>v0.3 doctor + scope-debug + status; v1.0 deep-audit reservation</td>\n<td>New DD</td>\n</tr>\n<tr>\n<td>DD-126</td>\n<td>OQ-v4-06</td>\n<td>Trust signals deferred to v0.4.1 (tentative)</td>\n<td>v0.3 FR-MARKETPLACE-4 SHA256 commit; M6 static-analysis gates</td>\n<td>New DD</td>\n</tr>\n<tr>\n<td>DD-127</td>\n<td>(post-M8 audit E9)</td>\n<td>Consent surface = `/coherence:consent` slash command, not TTY</td>\n<td>v0.3 `src/state/consent.ts#promptInteractive` placeholder</td>\n<td>New DD</td>\n</tr>\n<tr>\n<td>DD-128</td>\n<td>(post-M8 audit N4)</td>\n<td>`--out` path sandboxing (require in-tree or `--allow-out-of-tree` flag)</td>\n<td>v0.3 `src/commands/exportMetrics.ts`</td>\n<td>New DD</td>\n</tr>\n<tr>\n<td>DD-129</td>\n<td>OQ-v4-10</td>\n<td>Telemetry-gated trigger contract pattern</td>\n<td>v0.3 P8 bounded-read for `metrics.jsonl`; DD-104 + DD-116 carry</td>\n<td>New DD</td>\n</tr>\n<tr>\n<td>DD-130</td>\n<td>OQ-v4-04</td>\n<td>Slash-command-handler autogen deferred to v0.5+</td>\n<td>v0.3 docs-only handlers; conditional on OQ-v4-03 outcome</td>\n<td>Conditional DD</td>\n</tr>\n<tr>\n<td>TBD</td>\n<td>OQ-v4-05</td>\n<td>`bin/` folder PATH exposure</td>\n<td>v0.3 `bin/coherence-statusline.*` scripts</td>\n<td>New DD</td>\n</tr>\n<tr>\n<td>TBD</td>\n<td>OQ-v4-09</td>\n<td>Manifest field population (homepage / repository / license / keywords)</td>\n<td>v0.3 `plugin.json` fields</td>\n<td>New DD</td>\n</tr>\n<tr>\n<td>TBD</td>\n<td>OQ-v4-13</td>\n<td>npm package squat reaffirmation</td>\n<td>DD-093 (registry-only canonical)</td>\n<td>New DD</td>\n</tr>\n</table>\n*Numbering above is provisional and may shift; final allocation happens at the v0.4 spec-freeze gate.*\n— — —\n## Provisional draft DDs 2026-05-11 — proposals for v0.4 spec freeze\n*All entries below are 🟡 Draft pending v0.4 spec freeze. Each cites the motivating OQ-v4-NN, the rejected alternative, v0.3 codebase precedent (where applicable), the BRD slot, and an audit note.*\n- **🟡 DD-119** — **Manifest path = ****`.claude-plugin/plugin.json`****.** v0.3's `plugin.json` at the plugin root is moved into the `.claude-plugin/` subdirectory. Component directories (`skills/`, `agents/`, `commands/`, `hooks/hooks.json`, `.mcp.json`) stay at the plugin **root** — only the manifest moves. Motivated by OQ-v4-03 + Anthropic plugin schema ([https://code.claude.com/docs/en/plugins-reference#plugin-manifest-schema](https://code.claude.com/docs/en/plugins-reference#plugin-manifest-schema)). Rejected: keep manifest at root (fails `claude plugin validate`); ship both locations (drift surface). Precedent: none in v0.3 (this is the marketplace structural delta). Affects: TS-1 packaging, NFR-COMPAT-N4 extended. Audit: `npm run validate-plugin` ship-time gate (DD-123) catches regression.\n- **🟡 DD-120** — **State-storage tri-partition.** Three locations, three semantics:\n\t- `${CLAUDE_PLUGIN_DATA}` = `~/.claude/plugins/data/cohrence/` — **per-installation**: cassette cache, plugin-version cache, telemetry consent record\n\t- `.claude/coherence/` (gitignored) — **per-project, per-developer**: proposal-cache, scope-cache, signal-cache, session-map, metrics.jsonl, coherence-log\n\t- `coherence/` (committed) — **per-team**: plans, ignore, scope.json\n\tRule: data with project semantics stays per-project; data with team semantics stays committed; only data with pure per-installation semantics moves to `${CLAUDE_PLUGIN_DATA}`. Motivated by OQ-v4-01. Rejected: everything in `${CLAUDE_PLUGIN_DATA}` (breaks G-2 ignore + G-4 plans); everything in project tree (breaks the marketplace install model when multiple projects share an installation). Precedent: v0.3 DD-109 (signals stay per-developer); v0.3 `.gitignore` patcher in `firstRun.ts`. Affects: TS-3 schemas, NFR-PRIVACY-N5 carry, every state-write path. Audit: `tests/static-analysis/no-cross-dev-leak.test.ts` (M6) extended to assert tri-partition; static-analysis catches misplacement.\n- **🟡 DD-121** — **Version field strategy = explicit semver in ****`plugin.json`****.** Keep `\"version\": \"0.4.0\"` and bump on every release. Users get plugin updates only when the field bumps. Motivated by OQ-v4-08. Rejected: omit version (commit-SHA versioning — too noisy for installers; every commit treated as new version per Anthropic docs); marketplace-only version (drift surface). Precedent: v0.3 `\"version\": \"0.3.0\"` pattern. Affects: TS-1 release pipeline, plugin update UX. Audit: `release-ga.mjs` preflight checks `plugin.json#version` matches the pending git tag.\n- **🟡 DD-122** — **Manifest layout migration = strict re-install (no migrator).** When v0.4 detects a v0.3 `plugin.json` at the wrong location, emit a one-line CLI refusal (mirrors v0.3 `refuseLegacy.ts` shape) and exit cleanly without engaging degradedMode. Motivated by OQ-v4-02. Rejected: silent migration (violates DD-118); auto-move + warn (DD-118 reserves the right to require re-install for *every* major-version structural change). Precedent: v0.3 DD-118; v0.3 `src/state/refuseLegacy.ts` outcome pattern. Affects: NFR-COMPAT-N4 extended, FR-INSTALL-\\*. Audit: integration test covers refusal path; README install section updated to point at `/plugin install cohrence@claude-plugins-official`.\n- **🟡 DD-123** — **`claude plugin validate`**** is a ship-time gate in ****`scripts/release-ga.mjs`****.** Added to preflight as `npm run validate-plugin` between `npm run gates` and `npm test`. Failure halts release. Motivated by OQ-v4-03. Rejected: validate manually (regression-prone); validate in CI only (catches at PR time but not at release time). Precedent: v0.3 M6 ship-time gate pattern (`tests/static-analysis/`, `tests/ship/`). Affects: TS-8 release pipeline, M-VALIDATE-1 (new). Audit: meta-test asserts the gate trips when manifest is intentionally broken (mirrors v0.3 round-2 P7 meta-gates-trip pattern).\n- **🟢 DD-124** — **`parseMajor`**** formula correctness for ≥1.0.0.** Replace v0.3's `major*1000 + minor` arithmetic with a proper semver-aware comparator that treats `1.0.x` as a single major (not 1000 distinct ones). Ships in v0.4 because there is no installed base to migrate today; once any v1.0.x cut exists, the broken formula would fence v1.0.0 against v1.0.1 incorrectly. Motivated by post-M8 audit B2. Rejected: defer to v1.0 (creates a chicken-and-egg with the formula it would itself rely on). Precedent: v0.3 `src/state/refuseLegacy.ts#parseMajor` (broken formula on file). Affects: NFR-COMPAT-N4, refuseLegacy contract. Audit: unit tests cover 0.x.y, 1.0.0, 1.0.1, 2.0.0 boundaries; meta-test asserts `parseMajor('1.0.0') === parseMajor('1.0.99')`.\n- **🟡 DD-125** — **`/coherence:audit`**** v0.4 = bundling-only (NOT v1.0 deep audit).** v0.4's `/coherence:audit` runs `doctor` + `scope-debug` + `status` (and optionally a telemetry-export-summary) and renders a single readable report. Does NOT include v1.0-reserved deep audit functionality (token budget analysis, bloat warnings, cross-section consistency check). Motivated by OQ-v4-11. Rejected: ship deep audit (violates v1.0 reservation per BRD-5); skip entirely (loses cheap installer-self-diagnosis win). Precedent: v0.3 doctor + scope-debug + status command surfaces. Affects: TS-7 commands, BRD-5 v1.0 reservation. Audit: command output explicitly says \"v0.4 audit is a bundling-only summary; deep audit ships in v1.0\".\n- **🟡 DD-126** — **Trust signals (signed tarball, reproducible build, **[**SECURITY.md**](http://SECURITY.md)**, M6 gates as README claims) deferred to v0.4.1 (tentative ship-train slot).** Trigger condition: first-installer field signal indicates trust-worthy gaps. If no such signal accumulates within 30 days of v0.4 GA, scope rolls into v0.5. Motivated by OQ-v4-06. Rejected: ship in v0.4 (blows out the half-week milestone shape; trust-signal scope is better informed by real-installer signal); permanent v0.5 deferral (loses the fast-follow option if first-installer signal *does* indicate distrust). Precedent: v0.3 FR-MARKETPLACE-4 SHA256 commit (extends to signing in v0.4.1). Affects: BRD-5 §3, NFR-SECURITY-\\*. Audit: v0.4.1 trigger criteria documented in BRD-5 so the decision is deterministic, not ad-hoc.\n- **🟢 DD-127** — **Consent surface = ****`/coherence:consent`**** slash command (not TTY).** v0.3's `src/state/consent.ts#promptInteractive` is a placeholder — Claude Code hooks have no TTY, so the prompt never fires and defaults always apply (local ON, upload OFF). v0.4 surfaces consent as an explicit slash command that prints current state and accepts `--local on|off`, `--upload on|off`, `--reset` arguments. Motivated by post-M8 audit E9. Rejected: keep TTY placeholder (defaults always silently win); first-run modal (Claude Code surface doesn't expose modals to plugins). Precedent: v0.3 `/coherence:status` telemetry line. Affects: TS-7 commands, FR-PRIVACY-\\*, NFR-PRIVACY-N5. Audit: `/coherence:status` continues to surface current consent; `/coherence:consent` is the write path.\n- **🟢 DD-128** — **`/coherence:export-metrics --out`**** path sandboxing.** Default: `--out` paths must resolve inside the project root. To write outside, user passes explicit `--allow-out-of-tree` flag with security warning printed to stderr. Motivated by post-M8 audit N4. Rejected: silently accept any path (security review item); refuse all out-of-tree paths (blocks legitimate use case of exporting to a shared analysis directory). Precedent: v0.3 `src/commands/exportMetrics.ts`. Affects: TS-7 commands, NFR-SECURITY-N1 (new). Audit: integration test covers in-tree, out-of-tree without flag (refused), out-of-tree with flag (allowed + warning).\n- **🟡 DD-129** — **Telemetry-gated trigger contract pattern = env-flag default + readiness check at SessionStart.** Pattern: feature ships behind `COHERENCE_FEATURE=1` env flag (default OFF); SessionStart consults `metrics.jsonl` (using v0.3 P8 bounded-read primitive) to compute the readiness signal; when threshold met, emits a one-time CLI hint suggesting the user flip the env flag. No silent auto-flip — user retains control. Specific applications: DD-104 (planner promotion when ≥25% cross-kind co-occurrence over 30-day window); DD-116 (calibration re-tune trigger when ≥50 sessions × ≥30 days). Motivated by OQ-v4-10. Rejected: silent auto-flip (loses user control; violates v0.3 trust model); separate calibration job (introduces new substrate, no clear home). Precedent: v0.3 `COHERENCE_AUTHOR_PLANNER` env-flag pattern; v0.3 P8 bounded-read primitive. Affects: TS-2 hooks (SessionStart extension), TS-3 metrics surface. Audit: synthetic harness covers threshold-met / threshold-not-met / metrics.jsonl-absent cases; readiness check is deterministic for given input.\n- **🟢 DD-130** — *Slash-command-handler autogen deferred to v0.5+ (CONDITIONAL placeholder).* Motivated by OQ-v4-04. Status: open. Decision conditional on OQ-v4-03 outcome — if `claude plugin validate` requires runnable handlers (not just docs-only), pull autogen into v0.4 G-2 scope. Otherwise ship docs-only and defer autogen to v0.5+. Resolution path: run `claude plugin validate` against v0.3, capture verdict.\n— — —\n**DD register total (provisional):** v0.4 introduces DD-119..DD-130 (12 new). DD-130 is conditional pending OQ-v4-03. v0.4 BRD/Tech Spec authoring proceeds against the draft set; ratification at v0.4 spec-freeze gate.\n*Architectural commitments DD-117 (no backend, ever) and DD-118 (no legacy version support) carry forward unchanged. v0.4 introduces no new architectural commitments — all v0.4 work fits inside the v0.3 architectural envelope.*\n— — —\n<span color=\"green\">**Ratifications 2026-05-11 — promoted by v0.3 substrate landing in main**</span>\n*v0.3 implementation is complete (**`package.json`** / **`plugin.json`** / **`src/state/init.ts#PLUGIN_VERSION`** all at **`0.3.0`**; M0–M8 landed per **`docs/v0.3/CHANGELOG.md`**). The five DDs below are promoted from 🟡 Draft to 🟢 Ratified because v0.3 codebase evidence makes the decision deterministic. The remaining draft DDs (DD-119, DD-123, DD-124, DD-126, DD-127, DD-128, DD-130) stay 🟡 pending OQ-v4-03 (**`claude plugin validate`** baseline), OQ-v4-06 (v0.4.1 trigger), or implementation-time refinement.*\n- <span color=\"green\">**🟢 DD-120 ratified**</span> — **State-storage tri-partition rule** (closes OQ-v4-01). v0.3 substrate already implements the per-project + per-team split via `src/state/init.ts#getCoherenceDir` (`.claude/coherence/`), `src/state/plans/reader.ts` (`coherence/plans/<branch-sha>/*.json`), `src/state/consent.ts` (telemetry consent under `.claude/coherence/config.json#telemetry`), and `src/state/scope/cache.ts` (`scope-cache.json`). v0.4 adds the third tier `${CLAUDE_PLUGIN_DATA}` strictly for per-installation data without project semantics (plugin-version cache + per-installation flags). **Cassettes do NOT enter this tri-partition** — `src/llm/cassette.ts` is dev-only. Audit: `tests/static-analysis/no-cross-dev-leak.test.ts` (M6) is extended in v0.4 to assert every state-write call site lands in the correct tier; static-analysis catches misplacement at ship time.\n- <span color=\"green\">**🟢 DD-121 ratified**</span> — **Version field strategy = explicit semver** (closes OQ-v4-08). v0.3 ships `\"version\": \"0.3.0\"` in `plugin.json`, `package.json`, and `src/state/init.ts#PLUGIN_VERSION` consistently. The pattern is proven. v0.4 carries it forward with manual bumps; users get plugin updates only when the field bumps. Rejected: commit-SHA versioning (too noisy for installers per Anthropic plugin docs). Audit: `scripts/release-ga.mjs` preflight (DD-123) extended to assert all three version sources match the pending git tag.\n- <span color=\"green\">**🟢 DD-122 ratified**</span> — **Manifest layout migration = strict re-install** (closes OQ-v4-02). The v0.3 `refuseLegacy.ts` contract is the exact substrate. v0.4 extends `src/state/refuseLegacy.ts` with a parallel detector for old-layout `plugin.json` (root vs `.claude-plugin/`) emitting a one-line CLI refusal via the same `RefuseLegacyOutcome` shape (`status: 'refuse_layout'` discriminant added to the union). No migrator code path. v0.3 evidence: `REFUSE_LEGACY_MESSAGE` already establishes the messaging pattern (“... remove `.claude/coherence/` or run on a fresh project”). DD-118 (no legacy migration) governs; users re-install. Audit: integration test mirrors v0.3 `tests/unit/state/refuse-legacy.test.ts` coverage shape.\n- <span color=\"green\">**🟢 DD-125 ratified**</span> — **`/coherence:audit`**** v0.4 = bundling-only** (closes OQ-v4-11). v0.3 ships all four bundled handlers: `src/commands/doctor.ts`, `scopeDebug.ts`, `status.ts`, `exportMetrics.ts`. v0.4's `/coherence:audit` is a new command that invokes the four handlers in sequence and renders a single combined report. v1.0 deep-audit reservation honoured — v0.4 bundling is purely additive (no new analysis code). Audit: command output prints the line “v0.4 audit is a bundling-only summary; deep audit ships in v1.0” to set installer expectations.\n- <span color=\"green\">**🟢 DD-129 ratified**</span> — **Telemetry-gated trigger contract pattern = env-flag default + readiness check at SessionStart** (closes OQ-v4-10). v0.3 ships the proof-of-concept: `COHERENCE_AUTHOR_PLANNER` env-flag default-OFF (DD-104 ratified) with `src/state/metrics.ts` and the v0.2 P8 bounded-read primitive supplying the readiness substrate. v0.4 generalises this into a single `src/state/triggerContracts.ts` module that all telemetry-gated features consult at SessionStart; when threshold met, emits a one-time CLI hint suggesting the user flip the env flag (no silent auto-flip). Specific applications: DD-104 planner promotion threshold (≥25% cross-kind co-occurrence × 30-day window); DD-116 calibration re-tune trigger (≥50 sessions × ≥30 days). Audit: synthetic harness covers threshold-met / threshold-not-met / metrics.jsonl-absent cases.\n**Status-rule closures that do NOT consume a DD slot:**\n- <span color=\"green\">**🟢 OQ-v4-05 closed**</span> — `bin/` folder usage: no PATH exposure needed. v0.3's three statusline scripts (`coherence-statusline.ps1`, `coherence-statusline.sh`, `coherence-subagent-statusline.sh`) are invoked exclusively by Claude Code's statusline path written into `~/.claude/settings.json` by `src/commands/installStatusline.ts`. v0.4 `.claude-plugin/plugin.json` omits the `bin` field. Recorded as a v0.4 layout invariant alongside DD-119.\n- <span color=\"green\">**🟢 OQ-v4-09 closed**</span> — manifest field population: mirror `package.json` exactly. v0.4 `.claude-plugin/plugin.json` copies `author`, `license`, `repository`, `keywords`, `description` from `package.json` (already populated at the values cited in the Open Questions closure). No separate `homepage` field. This is a manifest-content delta only; folded into DD-119 implementation, no new DD slot.\n- <span color=\"green\">**🟢 OQ-v4-12 closed**</span> — cassette cache: N/A. `src/llm/cassette.ts` is dev-only (env-gated via `COHERENCE_CASSETTES_DIR` and `COHERENCE_REFRESH_CASSETTES`); no installer-facing cache exists. Subsumes the question; no DD allocation needed.\n- <span color=\"green\">**🟢 OQ-v4-13 closed**</span> — npm squat reaffirmed: claim the name as a placeholder; DD-093 (Anthropic plugin registry canonical) governs install paths. One-time external operation outside the plugin codebase; no DD slot.\n**Updated DD register total:** v0.4 introduces DD-119..DD-130 (12 new). **5 ratified** (DD-120, DD-121, DD-122, DD-125, DD-129). **7 stay 🟡 Draft** (DD-119 manifest path — awaits OQ-v4-03; DD-123 release-ga gate — awaits OQ-v4-03; DD-124 `parseMajor` correctness — awaits implementation; DD-126 trust signals — awaits OQ-v4-06 trigger; DD-127 consent slash command — awaits implementation; DD-128 `--out` sandboxing — awaits implementation; DD-130 slash-handler autogen — conditional on OQ-v4-03). DD-117 / DD-118 architectural commitments unchanged.\n**v0.4 BRD/Tech Spec authoring is unblocked for G-2 (ergonomics), G-3 (telemetry triggers), and G-4 (****`parseMajor`**** correctness).** G-1 (marketplace structural) authoring waits on the `claude plugin validate` baseline run.\n— — —\n<span color=\"green\">**Final ratifications 2026-05-11 — last red OQs resolved**</span>\n*Closes OQ-v4-03 / OQ-v4-04 / OQ-v4-06 / OQ-v4-07 with explicit DD entries (or recorded BRD rule). Each promotes the corresponding provisional draft DD from 🟡 to 🟢 with the resolution path baked in.*\n- <span color=\"green\">**🟢 DD-119 ratified**</span> — **Manifest path = ****`.claude-plugin/plugin.json`** (closes OQ-v4-03 part 1). Rationale: Anthropic plugin schema is publicly documented; the manifest-path target is known regardless of the baseline validate run. v0.3's root-level `plugin.json` moves into `.claude-plugin/`; component directories (`commands/`, `agents/`, `skills/`, `hooks/hooks.json`, `.mcp.json`) stay at plugin root. Rejected: keep manifest at root (fails `claude plugin validate`); ship both locations (drift surface). Affects: TS-1 packaging, NFR-COMPAT-N4 extended. Audit: paired with DD-123 ship-time gate.\n- <span color=\"green\">**🟢 DD-123 ratified**</span> — **`claude plugin validate`**** = tiered ship-time gate in ****`scripts/release-ga.mjs`** (closes OQ-v4-03 part 2). Schema/required-field errors halt release; warnings log to CI artifact (do not halt). Mirrors v0.3 M6 split (static-analysis hard gate + ship-time warnings informational). Rejected: full hard gate on all output (blocks release on advisory warnings); CI-warning-only (regression-prone at release time); manual run (catches regression late). Baseline-result capture becomes implementation task, not a spec blocker. Precedent: v0.3 `scripts/release-ga.mjs` preflight pattern; v0.3 round-2 P7 meta-gates-trip test. Affects: TS-8 release pipeline, M-VALIDATE-1 (new). Audit: validate-result cache keyed by sha256(manifest + component tree) skips re-run on unchanged tree; meta-test intentionally breaks `.claude-plugin/plugin.json` and asserts gate trips.\n- <span color=\"green\">**🟢 DD-130 ratified**</span> — **Slash-command-handler autogen pulled into v0.4 G-1** (closes OQ-v4-04; reverses provisional \"defer to v0.5+\"). Critical context: v0.3's `plugin.json#slashCommands[].handler` array is a non-standard Coherence invention. Anthropic plugin schema surfaces slash commands via `commands/<name>.md` files at plugin root. Without autogen, 25 v0.3 commands are invisible to marketplace installers. **Decision:** `scripts/generate-command-stubs.mjs` runs at `npm run build`, reads `plugin.json#slashCommands` (kept as the canonical command registry), emits one `commands/<name>.md` stub per entry. Each stub is a 5-line markdown file containing a sentinel pattern that the `UserPromptSubmit` hook intercepts and dispatches to the existing JS handler. No v0.3 implementation lost. Rejected: hand-write 25 markdown files (drift-prone); rewrite every command as pure markdown prompt (discards 2500+ lines of v0.3 logic); punt to v0.5 (ships v0.4 with no working slash commands). Precedent: v0.3 `src/hooks/userPromptSubmit.ts` already exists as a hook; sentinel-dispatch extends it. Affects: TS-1 build pipeline, TS-7 commands. Audit: static-analysis test asserts 1:1 mapping between `plugin.json#slashCommands[].name` and `commands/<name>.md`; build is idempotent (regenerates only when manifest hash differs); each stub is \\~50 tokens (≈1.3k total — immaterial vs typical context budget).\n- <span color=\"green\">**🟢 DD-126 ratified**</span> — **Trust signals deferred to v0.4.1 with tightened trigger criteria** (closes OQ-v4-06). Tentative ship-train slot; fires on any of: (1) ≥3 distinct installers request signed tarball / SBOM / reproducible build within 30 days of v0.4 GA; (2) public CVE in any direct dep requiring fast-patch beyond v0.4 patch cadence; (3) marketplace listing rejected or flagged for missing `SECURITY.md` / disclosure path; (4) supply-chain audit issue opened with severity ≥ medium. If none fire by v0.4 GA + 30 days, trust-signal scope rolls to v0.5. Rejected: calendared v0.4.1 (forces work regardless of demand); ship in v0.4 (blows milestone shape); unconditional v0.5 (loses fast-follow). Precedent: v0.3 FR-MARKETPLACE-4 SHA256 commit (extends to signing/SBOM in v0.4.1 if triggered). Affects: BRD-5 §3, NFR-SECURITY-\\*. Audit: criteria are deterministic boolean — no judgment call at decision time; each criterion has explicit evidence source (GitHub issues, CVE feed, marketplace email, audit issue); weekly manual triage covers the surface.\n**Recorded v0.4 BRD authoring rule (no DD slot, closes OQ-v4-07):**\n- **v1.0 references = placeholder + Roadmap-bullet inlining.** v0.4 BRD uses \"v1.0 (TBD — see [Roadmap](https://www.notion.so/5fd010d46a70821cbc6901ee992bbd5b))\" placeholder. BRD-5 §Non-goals lists the 5 v1.0-reserved features verbatim from the Roadmap (auto-apply, `asserts:` checking, quality-metrics dashboard, cross-session learning beyond 7-day, deep `/coherence:audit`). Every v0.4 deferral cross-links to its matching Roadmap bullet. New v1.0 reservations add to Roadmap, not BRD. Recorded as v0.4 authoring rule; parallel v1.0 scope-page authoring proceeds independently.\n— — —\n<span color=\"green\">**Implementation-time ratifications 2026-05-11 — final three DDs promoted**</span>\n*Ratifies the three DDs that required implementation-time refinement rather than OQ resolution. Each entry follows the alternatives → recommended decision → multi-axis audit pattern.*\n- **🟢 DD-124 ratified** — **`parseMajor`**** formula = ****`parseInt(version.split('.')[0])`** (fixes post-M8 audit B2). v0.3's `major*1000 + minor` arithmetic is wrong: it conflates minor bumps with major-bucket boundaries — `1.0.x` and `1.1.x` produce 1000 vs 1001, fencing them as if from different majors. **Alternatives considered:** (a) add `semver` npm dependency — correct but adds an external package to a zero-dependency plugin; (b) full custom semver comparator (\\~3 helpers) — overkill when `parseMajor`'s only contract is extracting the integer major digit; (c) `parseInt(version.split('.')[0])` — selected; (d) defer to v1.0 — chicken-and-egg: the broken formula would be consulted when deciding whether to honour the v1.0.x release. **Rejected:** (a) dependency cost; (b) over-engineering. Ships in v0.4 because no installed base exists today; the broken formula fences `1.0.0` against `1.0.1` incorrectly once any v1.0.x cut exists. Affects: `src/state/refuseLegacy.ts#parseMajor`, NFR-COMPAT-N4. **Audit:** *Performance* — O(1) string split, not on hot path (called ≤2× per SessionStart). *Quality* — fixes a real correctness bug before any ≥1.0.0 version exists; unit tests assert `parseMajor('1.0.0') === parseMajor('1.0.99')` and `parseMajor('1.0.0') !== parseMajor('2.0.0')`. *Token cost* — zero (no LLM). *Optimization* — result is trivially cacheable per string but `parseMajor` is called ≤2× per SessionStart; premature. *Completeness* — covers 0.x, 1.x, 2.x, ... buckets; no edge-case pre-release tags in current version scheme.\n- **🟢 DD-127 ratified** — **Consent surface = ****`/coherence:consent`**** slash command (not TTY)** (closes post-M8 audit E9). v0.3's `promptInteractive` placeholder never fires — no TTY in Claude Code hooks; defaults always apply silently (local ON, upload OFF). **Alternatives considered:** (a) keep TTY placeholder — silent defaults permanently win; (b) embed write path in `/coherence:status` — mixes read-only status display with write operations, confusing UX; (c) [CLAUDE.md](http://CLAUDE.md) config key — pollutes project-committed file with operational state; hard to discover; (d) env var `COHERENCE_TELEMETRY=on|off` — not discoverable; not persistent across shell sessions without manual profile config; (e) `/coherence:consent` standalone slash command — selected. **Decision:** no args → prints current consent state; `--local on|off` / `--upload on|off` / `--reset` → writes to `.claude/coherence/config.json#telemetry`. `/coherence:status` continues to display current consent (read path unchanged). Affects: `src/state/consent.ts`, `src/commands/consent.ts` (new), TS-7 commands, NFR-PRIVACY-N5. **Audit:** *Performance* — one `config.json` read + optional write; ≤1ms; not on hot path (user-initiated). *Quality* — explicit consent surfacing \\> silent defaults; discoverable via `/coherence:help`; write path is strictly user-initiated. *Token cost* — \\~200 tokens per invocation rendered to stdout; zero on all non-consent paths. *Optimization* — consent state already read at SessionStart for DD-129 `triggerContracts.ts`; no redundant I/O. *Security* — NFR-PRIVACY-N5 compliant; consent change logged with ISO timestamp in `config.json`. *Completeness* — covers `--local`, `--upload`, and `--reset` (returns to defaults: local ON, upload OFF).\n- **🟢 DD-128 ratified** — **`/coherence:export-metrics --out`**** = in-tree default + ****`--allow-out-of-tree`**** explicit override with stderr warning** (closes post-M8 audit N4). v0.3 `src/commands/exportMetrics.ts` accepts arbitrary `--out` paths without validation. **Alternatives considered:** (a) refuse all out-of-tree paths — breaks the legitimate use case of exporting to a shared team analysis directory; (b) accept any path silently — leaves N4 open; (c) allowlist config (user-configured acceptable dirs) — disproportionate complexity for the risk level; (d) in-tree default + `--allow-out-of-tree` + stderr warning — selected; follows \"secure default + explicit opt-in\" principle from v0.3. **Rejected:** (a) too restrictive for shared-dir workflows; (b) leaves N4 unaddressed; (c) over-engineered. Affects: `src/commands/exportMetrics.ts`, NFR-SECURITY-N1 (new). **Audit:** *Performance* — `path.resolve()` + `startsWith(projectRoot)` check is O(1); ≤1µs; negligible. *Quality* — closes N4; stderr warning gives actionable guidance and creates an audit trail for intentional boundary crossings. *Token cost* — zero (no LLM); warning ≤50 tokens to stderr. *Optimization* — path check folds into existing pre-flight validation; no new I/O pass. *Security* — prevents unintended writes to paths like `../../.ssh/`; `--allow-out-of-tree` is visible in shell history as an audit trace. *Completeness* — integration test covers 3 cases: in-tree (accepted), out-of-tree without flag (refused + message), out-of-tree with flag (accepted + stderr warning).\n**Final DD register total:** v0.4 introduces DD-119..DD-130 (12 new). **All 12 ratified** (DD-119, DD-120, DD-121, DD-122, DD-123, DD-124, DD-125, DD-126, DD-127, DD-128, DD-129, DD-130). DD-117 / DD-118 architectural commitments carry forward unchanged. **All v0.4 Open Questions are now closed.** v0.4 BRD/Tech Spec authoring is unblocked across all four goals (G-1 marketplace structural, G-2 first-impressions ergonomics, G-3 telemetry trigger contracts, G-4 `parseMajor` correctness).\n</content>\n</page>"
}